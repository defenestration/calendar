NS ?= abrevick
VERSION ?= latest

IMAGE_NAME ?= sinatra
CONTAINER_NAME ?= sinatra
CONTAINER_INSTANCE ?= default
CONFIG_PATH ?= ~/.config/$(IMAGE_NAME)
CONFIG_FILE ?= $(CONFIG_PATH)/config.yml
DB_FILE ?= $(CONFIG_PATH)/db.sqlite3

VOLUMES = -v $(CONFIG_FILE):/usr/src/app/lib/config.yml -v $(DB_FILE):/usr/src/app/db.sqlite3
CWDVOLUME = -v $(shell pwd):/usr/src/app
PORTS = -p 4567:4567
UIDS = -u 1000:1000